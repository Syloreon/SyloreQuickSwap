# Diagnostic dumps from discovery.lua (regenerated locally; not part of the mod)
discovery-*.txt

# User data — saved armor loadouts, created at runtime in the mod folder
loadouts.txt

# Internal dev/knowledge-transfer doc (has machine paths + working notes; keep local)
HANDOFF.md

# Logs / editor cruft
*.log
*.tmp
Thumbs.db
.DS_Store
sqs-hudgate.txt
